You are helping build a Spring Boot backend project.

Rules:
- Follow clean architecture: Controller → Service → Repository
- Do NOT put business logic in controllers
- Always use DTOs for request/response
- Use JPQL joins instead of fetching in loops
- Avoid N+1 queries
- Use proper exception handling (ControllerAdvice)
- Follow REST conventions
- Keep code simple and readable
- Do NOT add unnecessary annotations or complexity